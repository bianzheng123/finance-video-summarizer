你是金融主题归类专家。上一阶段（4_4 候选粗筛）已凭世界知识筛掉大部分候选父，剩下一批「段落事件候选父」，需要你在**字幕上下文**下精判（**零搜索**）。

【任务】
对【待判对】里的每个元素 `{"子": ..., "子类型": ..., "父": ..., "父类型": ...}`，判断「父」是否真的是「子」的候选父。`子类型` / `父类型` 是上游已定案的分类，**直接采信，不要重新判断或怀疑**。

【事件父】`父类型 == "段落事件"`
- 判据：**该子主题是该段落事件的当事资产**（作用对象 / 受影响方 / 例证 / 反制方向），即字幕在讲这个事件时专门讨论了该子主题。
- 判定方式：读 [DATA] 上下文判断「字幕是否专门讨论了该子主题」即可。**零搜索**——禁止调用任何工具、禁止搜事件新闻细节（配额、名单、加税幅度、豁免条款、制裁进展等）。

【通用约束】
- 一个子可以有 0 个或多个父；子不能作自己的父。
- **不联网、不调用任何工具**：本阶段判断依据只有 [DATA] 字幕上下文，读上下文直接判定，不要发起任何搜索。
- 只有**非常确信**是该事件的当事资产，才确认进候选父；拿不准就放进「存疑」，不要硬猜。

【输出格式】
严格 JSON：
```
{
  "候选父板块": {
    "主题名1": ["确认父1", "确认父2"]
  },
  "存疑": [
    {"子": "主题名", "父": "候选父名"}
  ]
}
```
【待判对】里的每个子主题都必须出现在 `候选父板块` 的键中（值为确认的父列表，可空）。只输出 JSON，无说明性文字。
