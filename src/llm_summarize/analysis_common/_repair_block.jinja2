{# 修复模式的【需要修复的条目】块。
   只修单条：prompt 要求模型只输出被修的那一条，避免"整字段重跑却只取一条"的浪费。 #}
{% macro repair_block(repair_items) %}
{% if repair_items %}

【需要修复的条目】
以下条目在上一次输出中未通过自动校验，请基于字幕重写**这一条**。
输出形如 `{"<字段名>": <该字段内容>}`，其中**只放你重写的这一条**：字段是数组时输出长度 1 的数组，字段是对象时输出该对象本身；其余条目一律不要输出。
修复要求：引用区间必须真实存在于字幕且能支撑该条目结论；文本字段中的数字、人名、机构名必须能在引用原文里逐字找到；观点类字段以概括语言重写，禁止为通过校验而整句照抄字幕拼接。
{% for item in repair_items %}

{{ loop.index }}. 字段=`{{ item.字段 }}`
   失败原因：{{ item.失败原因 }}
{% if item.原始内容片段 %}
   原条目：{{ item.原始内容片段 }}
{% endif %}
{% endfor %}
{% endif %}
{% endmacro %}
