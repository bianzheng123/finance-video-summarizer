[TASK]
【批量模式总则】
- 本批包含【本批主题清单】中的多个主题。对清单中**每一个**主题，分别输出一套完整的单主题分析（六个顶级字段），全部主题合并输出为一个 `主题` 数组——每个数组元素 = `主题名` + 六个顶级字段，`主题名` 必须与清单中的主题名**逐字一致**，六个顶级字段的结构与下文单主题规则里的【JSON 结构】完全一致。
- 除本总则外，下面的规则文本是**单主题规则**：规则中的「该主题」一律理解为「当前正在处理的这个主题」（即清单中该主题子块给出的主题名）。对每个主题，完整、独立地适用这些规则。
- **批量模式下本总则覆盖下方规则文本中与输出形态相关的表述**（下方【JSON 结构】与"最终只输出该 JSON 对象"只适用于单主题模式）：
  1. 顶层**只输出** `主题` 一个键，**不要**把六个顶级字段放在顶层；
  2. 下方【JSON 结构】描述的六个字段结构，仅适用于 `主题` 数组内**每个元素**的内部结构。
- 下方规则里的**判定标准**（铁律 A/B、立场判定、引用规则、去重规则等）全部照常适用，只是输出容器换成上面的 `主题` 数组。
- 清单中每个主题子块给出该主题的专属信息，逐主题遵守：
  * `行号范围`：该主题的分析依据——该主题的所有 `引用` rowID **必须**落在它自己的行号范围内（比 `[ROWID]` 并集更严格；并集只表示本批负责的整体范围，**不得**把其他主题行号范围里的内容写成该主题的观点）。
  * `子项白名单`：铁律 A / 铁律 B 的约束清单。
  * `个股清单`：`当前价格` 字段的输出清单。
  * `其他主题的引用区间`：即规则文本【本阶段的 [DATABASE] 语义】第 2 小节的逐主题版——`[DATABASE]` 中不再出现该小节，跨主题事件不重复展开的对照按主题子块执行。
- 主题之间严格隔离：不得把一个主题字幕里的观点/逻辑复制到另一个主题；黑话解码对照：某行 `关键词` 条目 `正式名` 等于某主题名（或其子项）时，该行只是**那个主题**的引用候选。
- 输出 JSON 形态示例（实际输出只含 `主题` 数组，无其他字段）：
  {"主题": [{"主题名": "清单中的主题名", "投资机会": [...], "个人交易记录": [...], "事件与资产": [...], "市场知识": [...], "观点": [...], "其他": [...]}, ...]}

---

{{ task_rules }}

---

【本批主题清单】
{% for topic in topics %}
### 主题「{{ topic.主题名 }}」
- 行号范围（该主题的分析依据，所有引用 rowID 必须落在这些行号内）：{{ topic.行号范围 | tojson }}
- 子项白名单：
{% if topic.子项白名单 %}
以下是已经分配给「{{ topic.主题名 }}」的子项清单——`投资机会` 的 `标题` 字段**必须**严格是 `{{ topic.主题名 }}` 自身或这些子项中的一个，**不能**额外添加其他资产名：
{{ topic.子项白名单 | tojson }}

铁律 A（必出）：上述子项白名单里**每一项**资产，都必须至少作为一个 `投资机会` 条目的标题输出（短线 + 中长线 都要出，无方向性观点时观点设为 `未表态 🤐`）。
铁律 B（不超）：`投资机会 标题` 必须严格 ∈ `{{ topic.主题名 }}` ∪ 上述子项白名单——字幕中提到的其他资产即使有方向性观点也只能写到 `市场知识`，不能作为 投资机会 条目。
{% else %}
主题「{{ topic.主题名 }}」当前没有子项——`投资机会` 的 `标题` 必须严格等于 `{{ topic.主题名 }}` 自身，**不能**为字幕中提到的其他资产创建 投资机会 条目（这些资产应归入 `市场知识 / 事件与资产 / 其他`）。
{% endif %}
{% if topic.关键词事件子项 %}

- 关键词事件子项：
以下是「{{ topic.主题名 }}」子项里属于**事件**的子项（上游已把它们降级为普通子项，不再单列主题、也不单列「投资机会」条目）。它们的完整事件叙事（起因→发展→影响→结果）**必须**写入 `事件与资产` 字段（可多条，按事件阶段展开），被提及/受影响的资产写进 `涉及资产`。这些子项**不得**作为 `投资机会` 的 `标题` 输出，也不受铁律 A/B 约束：
{{ topic.关键词事件子项 | tojson }}
{% endif %}
{% if topic.个股清单 %}

- 个股清单：
以下是子项白名单里属于个股的标的——`投资机会` 中以下标的的条目**必须**输出 `当前价格` 字段（取值：`贵 💰 / 便宜 💸 / 不贵也不便宜 ⚖️ / 未表态 🤐`，默认 `未表态 🤐`）：
{{ topic.个股清单 | tojson }}

白名单里的非个股子项（板块/概念）和 `{{ topic.主题名 }}` 板块自身条目**禁止**输出 `当前价格` 字段。
{% endif %}
{% if topic.兄弟主题区间 %}

- 其他主题的引用区间（该主题行号范围内与其他主题重叠的区间，供【跨主题事件不重复展开】对照）：
{{ topic.兄弟主题区间 | tojson }}
{% endif %}
{% endfor %}
[/TASK]

[DATA]
{{ subtitle_text }}
[/DATA]
{% if annotation_text %}
[ANNOTATION]
{{ annotation_text }}
[/ANNOTATION]
{% endif %}
[ROWID]
{{ rowid_scope | tojson }}
[/ROWID]
